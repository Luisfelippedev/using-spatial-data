<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://polyfill.io/v3/polyfill.min.js?features=default"></script>
    <script async src="https://maps.googleapis.com/maps/api/js?key=&libraries=geometry,places&callback=initMap">
    </script>
    <style type="text/css">
        
        #map {
            height: 100%;
        }
        
        html,
        body {
            height: 100%;
            margin: 0;
            padding: 0;
        }

          .container{
            position: fixed;
            z-index: 99;
            display: none;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;

        }
        .container.mostrar{
            display: flex;
            animation: down 0.5s;
        }

        form{
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            background: gray;
            padding: 2vh 5vw;
            border-radius: 20px;
            position: absolute;
        }

        .info{
            display: flex;
            flex-direction: column;
        }

        .titulo, .descricao{
            display: flex;
            flex-direction: column;
        }

        .titulo label, .descricao label{
            font-size: 12pt;
            text-align: center;
            margin-bottom: 1vh;
            margin-top: 1vh;
        }

        .titulo input{
            border: 1px solid white;
            border-radius: 75px;
            padding: 10px 5px;
        }

        .desc textarea{
            border: none;
            border-radius: 10px;
            padding: 5px 5px;
        }

        .salvo{
            padding: 2vh 2vw;
            font-size: 10pt;
            margin-top: 3vh;
            background-color: white;
            border: none;
            cursor: pointer;
            border-radius: 75px;
            transition: all ease 0.3s;
        }
        .salvo:hover{
            background-color: rgb(163, 57, 57);
        }
        
        .close{
            position: absolute;
            top: 2vh;
            right: 3vw;   
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: none;
            cursor:pointer;
            background-color: white;
            transition: all ease 0.3s;
        }
        .close:hover{
            background-color: rgb(163, 57, 57);
        }
        
    </style>

</head>
<body>
    {{{body}}}
</body>
</html>